doctype html 
html
    head 
        title WEB2 2LAB
        script(src="https://www.google.com/recaptcha/api.js" async defer)
    body
        h1 SQL Injection 
        p. 
         In computing, SQL injection is a code injection technique used to attack data-driven applications,
         in which malicious SQL statements are inserted into an entry field for execution (e.g. to dump the database contents to the attacker).
         users: Ivan, Marko, Josip; password: password
        h4 Medical records
        form(action="/submit" method="POST")
            label(for="name") Name:
                input(type="text" name="name" placeholder="Enter your name" required)
            br
            label(for="password") Password:
                input(type="password" name="password" placeholder="Enter your password" required)
            br
            label(for="checkboxId")
                | Vulnerability
                input#checkboxId(type="checkbox" name="vulnerability" value="yes")
            br
            button(type="submit") Submit
        hr
        h1 Broken Authentication
        p username: user1, password: password123 / IT54wetYsLzMWM8 (ovisno o ranjivosti)
        form(action="/login" method="POST")
            label(for="username") Username:
            input#username(type="text" name="username" required)
            br
            label(for="password") Password:
            input#password(type="password" name="password" required)
            br
            .g-recaptcha(data-sitekey=siteKey)
            br
            label(for="checkboxId") Vulnerability
            input#checkboxId(type="checkbox" name="vulnerability" value="yes")
            br
            button(type="submit") Login